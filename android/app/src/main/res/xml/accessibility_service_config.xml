<?xml version="1.0" encoding="utf-8"?>
<!--
    AccessibilityService configuration for AI Guardian.

    Event types:
    - typeWindowStateChanged: foreground app detection + policy evaluation
    - typeViewTextChanged: text content filtering (user input)
    - typeWindowContentChanged: text content filtering (content updates)

    canRetrieveWindowContent is ON to allow node-tree text extraction
    when event.text is empty.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_service_description"
    android:accessibilityEventTypes="typeWindowStateChanged|typeViewTextChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:settingsActivity="com.aiguardian.ai_guardian.MainActivity" />
