package com.aiguardian.ai_guardian.storage

/**
 * Simple SharedPreferences wrapper for AI Guardian user-facing settings.
 *
 * Currently empty — voice/TTS/AI settings were removed.
 * Keep as placeholder for future settings.
 */
object SettingsStorage {
    private const val TAG = "AIGuardianSettings"
    private const val PREFS_NAME = "aiguardian_settings"
}
